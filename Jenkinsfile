pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                bat "git clone https://github.com/AmeyDevarshi/SampleProject.git"
            }    
    }
        
}
